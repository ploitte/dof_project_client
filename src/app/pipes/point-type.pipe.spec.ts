import { PointTypePipe } from './point-type.pipe';

describe('PointTypePipe', () => {
  it('create an instance', () => {
    const pipe = new PointTypePipe();
    expect(pipe).toBeTruthy();
  });
});
