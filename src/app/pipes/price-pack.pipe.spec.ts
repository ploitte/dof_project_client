import { PricePackPipe } from './price-pack.pipe';

describe('PricePackPipe', () => {
  it('create an instance', () => {
    const pipe = new PricePackPipe();
    expect(pipe).toBeTruthy();
  });
});
